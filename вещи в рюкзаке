from itertools import combinations

def find_valid_combinations(items, max_weight):
    valid_combinations = []
    
    for r in range(1, len(items) + 1):
        for combo in combinations(items.items(), r):
            total_weight = sum(item[1] for item in combo)
            if total_weight <= max_weight:
                valid_combinations.append(combo)
    
    return valid_combinations

valid_combinations = find_valid_combinations(items, max_weight)

# Возврат одного допустимого варианта
# single_valid_combination = valid_combinations[0] if valid_combinations else None

# Возврат всех возможных вариантов
# all_valid_combinations = valid_combinations

